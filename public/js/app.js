const element = document.getElementsByClassName("sa")[0];
let parsedNo = parseInt(element.textContent);
element.addEventListener("click", function() {
  // parsedNo++;
  // element.innerText = parsedNo;
});
